// Initialize an empty array to hold task objects.
// This acts as a lightweight in-memory database for the app.
let tasks = [];

// Export the array so it can be accessed and modified in other files (e.g., controllers).
module.exports = tasks;
